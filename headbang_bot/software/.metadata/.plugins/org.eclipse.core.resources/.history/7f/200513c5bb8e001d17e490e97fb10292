#include <stdio.h>
#include <system.h>
#include <io.h>
#include <unistd.h>
#include <string.h>

int main(void)
{
	signed int ADC_DATA_BUFF[32] = {};
	unsigned int ADC_CLK_HIGH = 0;

	while(1)
	{


		IORD(ADC_LR_CLK_BASE, 0)
		if()
		ADC_DATA_BUFF = IORD(AUD_DAT_BASE, 0);
	}
}
